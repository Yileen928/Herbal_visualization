<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.herbal.visualization.mapper.MedicineStoreMapper">
    <select id="getProvinceCounts" statementType="CALLABLE" resultType="cn.herbal.visualization.model.ProvinceCount">
        CALL GetProvinceCounts()
    </select>

    <select id="getTypeCounts" statementType="CALLABLE" resultType="cn.herbal.visualization.model.TypeCount">
        CALL GetTypeCounts()
    </select>
</mapper>